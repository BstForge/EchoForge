<UserControl x:Class="EchoForge.Panes.AudioFilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             Background="#1e1e1e">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="AUDIO FILES VIEW"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,10" />

        <StackPanel Grid.Row="1">
            <TextBlock Text="Audio Files"
                       FontWeight="Bold"
                       Foreground="LightGray" />
            <ListBox MinHeight="150"
                     Background="#262626"
                     Foreground="LightGray"
                     Margin="0,5,0,10" />

            <TextBlock Text="Controls"
                       FontWeight="Bold"
                       Foreground="LightGray" />
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <Button Content="Play" Margin="0,0,10,0" Padding="10,4" />
                <Button Content="Stop" Padding="10,4" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

